import {Schema} from './Schema';


class Parser{
    constructor(schema){
        this.schema =schema;
    }
    parse(args){
        args.array.forEach(element => {
            args.type;
        });
    }
}