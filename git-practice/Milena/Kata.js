class Args{
    constructor(schema, args){
        this.args = args;
        this.schema = schema;
    }

    parse(){
        if(this.schema.length === 0 && this.args.length ===0){
            return;
        }
        this.parse

    }

}

