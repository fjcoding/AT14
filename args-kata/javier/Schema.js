class Schema{
    constructor(LineCommads){
        parse(LineCommads)
    }
    parse(LineCommads){
        LineCommad = LineCommads.split(' ');
        LineCommad.array.forEach((element, index) => {
            
        });
    }
}